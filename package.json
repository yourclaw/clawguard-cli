{
  "name": "@yourclaw/clawguard-cli",
  "version": "0.2.0",
  "description": "Scan AI agent skills for security vulnerabilities",
  "bin": {
    "clawguard": "bin/clawguard.js"
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsup src/cli.ts --format cjs --dts",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "biome check src/",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist/",
    "bin/"
  ],
  "dependencies": {
    "@yourclaw/clawguard-rules": "^0.1.0",
    "@yourclaw/clawguard-scanner": "^0.3.0",
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.0",
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourclaw/clawguard-cli.git"
  }
}
